from setuptools import setup, find_packages

setup(
    name='tabler_django',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'Django',
        # Add other dependencies here
    ],
)
